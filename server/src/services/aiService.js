async function generateReply(message) {
  // Placeholder implementation.
  // Replace with a real provider (OpenAI, etc.) when ready.
  return `You said: ${message}`;
}

module.exports = {
  generateReply,
};

